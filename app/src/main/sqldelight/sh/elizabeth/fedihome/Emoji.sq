CREATE TABLE EmojiEntity (
    emojiId TEXT PRIMARY KEY NOT NULL ,
    instance TEXT NOT NULL,
    shortcode TEXT NOT NULL,
    url TEXT NOT NULL
);

insertOrReplace:
INSERT OR REPLACE INTO EmojiEntity VALUES ?;
